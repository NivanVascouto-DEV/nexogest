// PRODUÇÃO: troque a URL abaixo pela URL real do backend publicado no Render
// (algo como https://nexogest-backend.onrender.com) assim que o deploy estiver
// pronto — a URL exata só existe depois de criar o serviço no Render, então
// esse valor precisa ser atualizado manualmente aqui (ver DEPLOY.md).
//
// Para testar localmente antes do deploy, use http://localhost:3000, ou o IP
// da sua máquina na rede (ex: http://192.168.1.17:3000) para acessar de outro
// dispositivo como celular.
const API_URL = 'https://SEU-BACKEND.onrender.com';
